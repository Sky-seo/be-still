server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = / {
        return 302 /be-still/;
    }

    location /be-still/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /be-still/index.html;
    }
}
